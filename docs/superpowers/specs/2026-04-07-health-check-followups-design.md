# 2026-04-07 健康检查后续待改项设计

本文档只记录健康检查结论之后，已确认值得做、但不在本轮主实现中落地的两项工作。

## 待改 A：健康检查命令固化

### 目标

把当前“项目健康检查”的最小验证命令，固化成一个可以直接执行的入口，而不是继续依赖人工记忆。

### 预期产出

- 一个统一命令或脚本入口
- 输出至少包含：
  - acceptance 主链结果
  - Phase 4 核心回归结果
  - 成功 / 失败摘要

### 价值

- 降低接手成本
- 降低“我忘了该跑哪组测试”的概率
- 让健康检查从文档建议变成实际动作

### 当前状态

- `pending`

## 待改 B：真实样例抽检与复盘模板

### 目标

把当前分散在 README、SOP、Sequence Checklist 里的样例复盘方法，整理成一套固定模板。

### 预期产出

- 一份面向真实样例试跑的 SOP 或模板
- 至少覆盖：
  - `qa-overview.md`
  - `action-sequence-routing-metrics.json`
  - `sequence-qa-metrics.json`
  - `video-metrics.json`
  - `delivery-summary.md`

### 价值

- 让每一轮样例复盘都能留下同结构结论
- 更快识别：
  - 是参考素材问题
  - 是 prompt 问题
  - 是 QA 判定问题
  - 还是 composer 覆盖没有真正生效

### 当前状态

- `pending`

## 当前执行顺序

默认顺序固定为：

1. 先做本轮 `Phase 4 Sequence QA 通过率优化`
2. 再补 `A：健康检查命令固化`
3. 再补 `B：真实样例抽检与复盘模板`
